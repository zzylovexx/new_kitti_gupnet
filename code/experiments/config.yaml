random_seed: 444

dataset:
  type: 'kitti'
  root_dir: 'Dataset/'
  batch_size: 8
  class_merging: False
  use_dontcare: False
  use_3d_center: True
  writelist: ['Car']
  random_flip: 0.5
  random_crop: 0.5
  scale: 0.4
  shift: 0.1
  use_2dgt: True
model:
  type: 'gupnet'
  backbone: 'dla34'
  neck: 'DLAUp'

optimizer:
  type: 'adam'
  lr: 0.00125
  weight_decay: 0.00001

lr_scheduler:
  warmup: True  # 5 epoches, cosine warmup, init_lir=0.00001 in default
  decay_rate: 0.1
  decay_list: [90, 120]

trainer:
  max_epoch: 200
  eval_frequency: 10 # evaluation interval (in epoch)
  save_frequency: 10 # checkpoint save interval (in epoch)
  disp_frequency: 5 # display interval (in batch)
  # log_dir: 'log/'
  log_dir: '/media/chang0731/512ssd/new_kitti_gupnet/kitti_gupnet_checkpoint/'
  save_dir: 'origin_2dgt_backup/'

  # resume_model: '/media/chang0731/512ssd/kitti_gupnet_checkpoint/origin_projection_loss_affine_flip_depth/checkpoint_epoch_90.pth' 
tester:
  threshold: 0.2  # confidence filter
  use_2dgt: True
  resume_model: '/media/chang0731/512ssd/new_kitti_gupnet/kitti_gupnet_checkpoint/origin_2dgt_backup/checkpoint_epoch_200.pth' 
  # resume_model: 'log/origin_1class/checkpoint_epoch_140.pth' 
  save_dir : 'origin_2dgt_backup/'
  # GUPNet/code/outputs_dir/origin_projecion_affine_flip_camera_good_test